"""``comms.lib`` -- foundational helpers vendored alongside the comms contract.

    misc    dependency-light helpers (frames, geometry, pose, pngio)
    config  resolution-aware vision tuning (project-math glue; NOT used by comms)

These are imported by comms's own modules (e.g. ``comms.runtime`` is reused by
``comms`` package is copy-pastable into any project unchanged.
"""
