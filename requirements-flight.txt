# Lean FLIGHT install for the Pi (no OpenCV, no Qt/UI, no calib/oracle deps).
#
# The from-scratch VIO/SLAM flight runtime is library-free by design: own
# KLT/PnP/corners, own ORB loop closure, own SGM stereo + dense direct VO, a
# pure-Python PNG codec, and factory calibration (no on-device cv2 calibrate).
# So the heavy `opencv-python` wheel is NOT installed here -- proven by
# `python -m verification.cv2_absent_flight_litmus` (the full --vl53l9cx --direct
# replay runs imu_camera -> vio -> slam at rc=0 with cv2 unimportable).
#
# Use the full `requirements.txt` for development, the Qt UI, the calibration
# wizard, the dev-only PnP A/B oracle, and the parity/oracle tooling -- those
# still use OpenCV.

# Numerics: every flight math op (gradients, pyramids, area-resize, stereo
# denoise, ORB, PnP, KLT) is implemented on top of NumPy.
numpy>=2.0

# JIT-accelerates the pure-NumPy hot paths (SGM cost volume / speckle / median,
# optical flow) so the library-free frontend runs in real time. Strongly
# recommended on the Pi; the code falls back to pure NumPy if it is absent.
numba>=0.61

# IMU serial transport.
pyserial>=3.5

# Downward rangefinder (VL53L1X over I2C) for the `lidar` process -- the AGL range
# the fc sender bundles into the dblink VIO-pose frame. NO sensor driver is pinned
# in the lean flight image: the `lidar` process runs DEVICELESS with `--lidar-mock`
# (needs nothing). The REAL VL53L1X read is a BENCH BRING-UP item -- there is no
# drop-in pip driver for the Pi5 (aarch64 + py3.13): `pimoroni-vl53l1x` does NOT
# exist on PyPI (the package is `vl53l1x`), and `vl53l1x` (the Pimoroni C-extension)
# has NO aarch64/py3.13 wheel on piwheels (only 32-bit armv6l/v7l up to cp311), so
# it will not install on the Pi5 without a from-source C build the old code can't do
# on py3.13. Pick a pure-Python driver (smbus2 register access) or Adafruit Blinka
# at bring-up -- see lidar/requirements.txt. smbus2 (the I2C transport) is the only
# bit that installs cleanly, kept here for whichever real driver is chosen:
smbus2>=0.4.1

# OAK-D / camera device driver -- needed for `--live` on the actual board (the
# real ToF/stereo source). Omit it if the Pi only ever replays recorded sessions.
# PIN <3.7: depthai 3.7.x ships a device-firmware regression in the MIPI camera
# source plugin that CRASHES the OAK-D Lite on stream start (RTEMS_FATAL_SOURCE_
# INVALID_HEAP_FREE in PlgSrcMipi.cpp -> repeated X_LINK_ERROR / "Device crashed",
# at ANY resolution). 3.6.x streams the Lite fine. NOT a power/cable issue (the
# device idles + bare-opens stable; only camera streaming trips it). See
# docs/RPI5_DEPLOY.md §1b. Revisit when depthai fixes the 3.7 firmware.
depthai>=3.6,<3.7
